# Synthetic test fixtures

This directory intentionally contains no real official snapshot. Any future fixture must be explicitly marked `synthetic` or `test-only` and must never be loaded by production paths.

The `it-passport-*.html` files are synthetic fixtures for the IT Passport offline adapter.
