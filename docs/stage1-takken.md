# 阶段 1：宅建数据闭环（当前轮）

当前轮只覆盖 `takken`。`source:takken:retio-exam` 登记了产品方案中的官方入口，但本轮不访问网络；所有测试数据均来自 `fixtures/official-snapshots/takken-synthetic.json`，并带有 `synthetic`/`test_only` 标记。

已完成一次受控真实读取，确认主页使用普通 HTML 链接；解析器当前只提取同域已登记入口，不从公告文本直接推断日期、费用或合格率。

已完成日程页候选解析：按 `インターネット申込み`、`試験日時`、`合格発表` 标题分段，提取 JST 日期/时间候选；所有候选固定为 `high` + `pending_review`，不会自动批准。

闭环：快照哈希 → 目标字段差异 → 候选事实 → 风险/来源冲突标记 → reviewer 审核 → revision → 构建成功后公开 → 失败保留旧版 → 回滚。

已覆盖：页面变化但事实不变、考试日期高风险、来源冲突、未认证审核、批准幂等、公开读取、构建失败保留旧版、回滚。

本轮未做：实时 HTTP、重定向/SSRF 校验、PDF、Playwright、生产对象存储、后台 UI、ITパスポート、正式用户页面模板。
