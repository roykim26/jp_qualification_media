# Local-only configuration. Never commit real credentials or production endpoints.
DATABASE_URL=postgresql://qualification_dev:qualification_dev@localhost:5432/qualification_media
OBJECT_STORAGE_ENDPOINT=http://localhost:9000
OBJECT_STORAGE_BUCKET=official-snapshots-local
OBJECT_STORAGE_ACCESS_KEY=local-only-placeholder
OBJECT_STORAGE_SECRET_KEY=local-only-placeholder
API_HOST=127.0.0.1
API_PORT=4100
NODE_ENV=development
# Production credentials, production databases, and public deployment URLs are intentionally excluded.
